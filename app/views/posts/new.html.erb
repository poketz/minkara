<div class="container">
  <h3 class="mt-4">新規投稿</h3>
  <p class="alert alert-danger mt-4"><strong>注意事項</strong><br>
  投稿には必ず利用許諾のある音源を使用してください。<br>
  カラオケ音源を無断で使用する事は著作権で禁じられています。<br>
  著作権違反の投稿はを管理者が見つけ次第削除いたします。</p>

  <%= form_with model: @post, url: posts_path do |f| %>

    <div class="field">
      <%= f.label :audio %>
      <%= f.file_field :audio %>
    </div>

    <div class="field">
      <%= f.label :artist_name %>
      <%= f.text_field :artist_name %>
    </div>

   <div class="field">
      <%= f.label :song_name %>
      <%= f.text_field :song_name %>
    </div>

    <div class="field">
      <%= f.label :poster_comment %>
      <%= f.text_field :poster_comment %>
    </div>

    <%= f.submit "投稿する" %>
  <% end %>
</div>