<%= form_with model: @post_comment, url: post_post_comments_path(post_id: @post.id), local: false do |f| %>
  <%= f.text_area :body, id: 'post_comment' %>
  <div><%= f.submit 'コメントする' %></div>
<% end %>